interface:
  display_name: "Commit Code"
  short_description: "Write changelog, split runs and skill commits, then merge to main"
  default_prompt: "Use $commit-code to write the changelog, commit runs/ changes to the runs branch, commit other skill changes to the skill branch, then merge both branches into main."
